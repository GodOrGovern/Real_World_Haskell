Load your "lastButOne" function into ghci and try it out on lists of different
lengths. What happens when you pass it a list that is too short?

An exception (Non-exhaustive patterns in function lastButOne) occurs when a
list with length < 2 is passed as a function parameter
